# Environments
.env
.venv/
env/
venv/
ENV/

# Python Cache
__pycache__/
*.py[cod]
*$py.class

# Application Data & State
# chroma_db/          ← included so collaborators get the pre-built database
# database_images/    ← included so collaborators get extracted diagrams
# processed_files.json ← included for incremental ingestion tracking

# Textbooks / Documents (Ignore large PDFs but keep the folder if needed)
Document Loaders/*
!Document Loaders/.gitkeep

# OS specific files
.DS_Store
Thumbs.db
